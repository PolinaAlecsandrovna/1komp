-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.partners
(
    partner_id bigint NOT NULL,
    partner_type_id bigint NOT NULL,
    name character varying(150) COLLATE pg_catalog."default" NOT NULL,
    city_id bigint NOT NULL,
    street character varying(70) COLLATE pg_catalog."default" NOT NULL,
    number_home integer NOT NULL,
    inn integer NOT NULL,
    director_name character varying(50) COLLATE pg_catalog."default" NOT NULL,
    director_surname character varying(70) COLLATE pg_catalog."default" NOT NULL,
    director_patronymic character varying(70) COLLATE pg_catalog."default",
    phone integer NOT NULL,
    email character varying(250) COLLATE pg_catalog."default" NOT NULL,
    logo character varying(600) COLLATE pg_catalog."default",
    rating integer,
    CONSTRAINT partners_pkey PRIMARY KEY (partner_id)
);

CREATE TABLE IF NOT EXISTS public.products
(
    product_id bigint NOT NULL,
    article character varying(50) COLLATE pg_catalog."default" NOT NULL,
    type_id bigint NOT NULL,
    name character varying(200) COLLATE pg_catalog."default" NOT NULL,
    description text COLLATE pg_catalog."default",
    image bit varying(600),
    min_price_partner money NOT NULL,
    package_lenght integer,
    package_width integer,
    pckage_height integer,
    weight_without_package integer,
    weight_with_package integer,
    certificate_path character varying(600) COLLATE pg_catalog."default",
    standard_number integer,
    production_time_hours integer,
    cost_price money,
    workshop_number integer,
    workers_count integer,
    production_materials character varying(700) COLLATE pg_catalog."default",
    CONSTRAINT products_pkey PRIMARY KEY (product_id)
);
END;
